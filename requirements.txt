# Core runtime requirements (API / service)
fastapi>=0.110,<0.115
uvicorn[standard]>=0.24,<0.28
# Install torch/torchaudio compatible with your platform from https://pytorch.org/get-started/locally/
torch
torchaudio

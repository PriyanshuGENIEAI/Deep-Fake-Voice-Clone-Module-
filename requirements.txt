# Core runtime requirements (API / service)
fastapi>=0.110,<0.115
uvicorn[standard]>=0.24,<0.28

# Chatterbox TTS (pulls transformers, diffusers, etc.)
chatterbox-tts==0.1.6

# Torch stack (pin to match chatterbox-tts 0.1.6 tested environment)
torch==2.6.0
torchaudio==2.6.0
